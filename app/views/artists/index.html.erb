<div class="container">
  <div class="cards">
    <% @artists.each do |artist| %>
      <div class="link-card">
        <%= link_to artist_path(artist) do %>
          <div class="card-trip">
            <img src= <%= artist.photo %>/>
            <div class="card-trip-infos">
              <h2> <%= artist.name %></h2>
              <p> <%= artist.city %> </p>
              <img src= <%= artist.avatar %> class= "card-trip-user avatar-bordered" />
            </div>
          </div>
        <% end %>
      </div>
    <% end %>
  </div>

  <%= link_to "Ajouter un tatoueur", new_artist_path %>
  <%= link_to "Précedent", root_path %>
</div>
